# Railway/Nixpacks configuration
# Use Node 22 to satisfy dependency requirements

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm i -g npm@11", "npm ci"]

[phases.build]
cmds = ["NEXT_TURBOPACK_EXPERIMENTAL_USE_SYSTEM_TLS_CERTS=1 npm run build"]

[start]
cmd = "npm start"
